import type { ToasterProps } from "sonner";


export const sonnerAdapter: ToasterProps= {
  position: 'top-center',
  duration: 2500,
  style: {
    background: '#000',
    color: '#fff',
  },
  richColors: true,
}